# https://www.codewars.com/kata/576b93db1129fcf2200001e6

def sum_array(arr):
    try:
        if len(arr) > 1:
            arr.sort()
            del arr[-1]
            del arr[0]
            return sum(arr)
        else: return 0
    except:
        return 0
